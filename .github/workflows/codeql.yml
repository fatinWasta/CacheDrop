name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '29 8 * * 5'

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
          - language: swift
            build-mode: manual   # switched to manual build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}

      # Manual build steps for Swift
      - name: Build Swift project
        if: matrix.language == 'swift' && matrix.build-mode == 'manual'
        shell: bash
        run: |
          # Clean Derived Data to avoid CI caching issues
          rm -rf ~/Library/Developer/Xcode/DerivedData/*

          # Install dependencies if using CocoaPods
          if [ -f Podfile ]; then
            pod install
          fi

          # Build the main app target in Release mode
          xcodebuild -workspace CacheDrop.xcworkspace \
                     -scheme CacheDrop \
                     -configuration Release \
                     -sdk iphonesimulator \
                     CODE_SIGN_IDENTITY="" \
                     CODE_SIGNING_REQUIRED=NO \
                     clean build

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{matrix.language}}"
